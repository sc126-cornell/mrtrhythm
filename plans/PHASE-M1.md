# Phase 計畫 — M1：資料管線

| 項目 | 內容 |
|---|---|
| 狀態 | 🟠 檢查完成（待用戶 Gate 確認進 M2） |
| 預估 / 實際耗時 | 7 人日 / 約 1 人日 |
| 對應任務 | TASKS.md T1.1–T1.6 |
| 開始 / 完成日期 | 2026-07-18 / 2026-07-18 |

## 1. 目標（Plan）

1. TDX 原始資料 → `web/public/data/network.json`：11 條交路的幾何（Shape 縫合＋切片）、車站、站間秒數，幾何無斷裂、無鋸齒繞行。
2. StationTimeTable → `tt-{weekday|sat|sun}.json`：按（交路×方向）串班，認領率 ≥95%；文湖線以 Frequency 合成（標記 synthetic）。
3. `validate.ts` 驗證器（門檻不過 exit 1，供 CI 使用）＋ debug 疊圖頁（隨站部署，瀏覽器直接目檢）。

**可展示物**：https://mrtrhythm.vercel.app/debug.html 看到全路網幾何與車站；驗證報告全綠。

執行順序依垂直切片：每一步先讓淡水信義線（R-1/R-2/R-3）通過，再放大到全網。

## 2. 工作項目（Do）

- [x] T1.1 `pipeline/fetch.ts`：7 個靜態端點抓取器＋當日快取（raw/ 已有今日資料則跳過）、15s 節流、429 退避（2026-07-18 完成）
- [x] T1.2 `pipeline/build-network.ts`：WKT 解析 → 縫合 → 車站投影 → 按交路切片組裝 → network.json；含 debug 疊圖頁（2026-07-18 完成：11 交路長度與官方相符（R-1 28.3km、G-3 1.9km、BR-1 25.1km）；縫合需**轉角約束 75°**（否則支線黏進主線）＋**跨鏈橋接**（大橋頭／七張交會）——兩個坑都已解；殘餘 4 筆警告＝月台座標離軌道 85–123m 的資料特性（BR09、O17），非幾何錯誤）
- [x] T1.3 `pipeline/build-timetable.ts`：串班完成（2026-07-18）。兩輪演算法演進：①按終點群組裁切（發現 BL-2 昆陽班、R-2 實為北投↔象山）②中途起點 cascade（撈回平日約 190 班出庫車）。認領率 99.7–100%（門檻 95%）
- [x] T1.4 文湖線合成：平日 202 班/方向、假日 151 班/方向，`synthetic: true`（2026-07-18）
- [x] T1.5 交路完整性：平日 11/11；假日 10/11——**G-2 僅平日行駛**（營運事實，非缺漏），驗證器按日別允許
- [x] T1.6 `pipeline/validate.ts`：結構／單調性／涵蓋／量級門檻自動化，exit code 供 CI（2026-07-18 全綠）

## 3. Verify — 技術驗證（對照 SDD §4）

- [x] fetch：快取命中零 API 呼叫（實跑：7 端點全跳過、0 新抓）；節流／退避程式路徑與 Spike 同款已實戰
- [x] network.json：11 交路齊全；長度與官方相符（R-1 28.3km、G-3 1.9km、BR-1 25.1km）；殘餘 4 筆警告＝月台座標偏移 85–123m 資料特性
- [x] debug 頁人工目檢：五線＋三支線＋O 雙尾正確，無斷裂、無跨河直線（本人與用戶各檢一次）
- [x] 串班：認領率 99.7–100%（門檻 95%）；雙認領由 claimed 旗標排除＝0；validate.ts 站間倒退＝0、>15min 長間隔＝0
- [x] BR 合成：平日 202 班/方向（尖峰班距 ~2 分、離峰 ~4–7 分展開合理）；validator 量級檢查 200–600 通過
- [x] `npm run lint`、`npm run build` 綠燈；validate.ts ✅ 全數通過
- [x] 傳輸量：gzip 後 network.json 32KB＋tt-weekday.json 189KB，遠低於 SDD §8 預算 600KB

## 4. Validate — 需求驗收（對照 PRD F1/F2 的資料前提）

- [x] 用戶開 debug 頁目檢路網（2026-07-18 用戶回覆「我覺得很好」）
- [x] 平日 2,955 班／週六 2,484 班／週日約 2,400 班（含 BR 合成），與北捷公開的每日約三千班量級相符
- [ ] 用戶同意資料品質足以進 M2（列車動畫）——隨本 phase Gate 一併確認

## 5. Check 紀錄（2026-07-18 填寫）

### Code Review
- 範圍：fetch.ts、build-network.ts、build-timetable.ts、validate.ts、debug.html。ESLint 零發現、`npm run build` 綠燈。
- 發現與處置：
  1. tt-*.json 未壓縮 1.0–1.3MB（gzip 後 189KB 內）——在預算內；若 M2 載入體感慢，備案為緊湊陣列格式（記入 M2 注意事項）。
  2. BR 合成班距取（Min+Max）/2 近似——精度由 M4 看板校正吸收，已標記 synthetic。
  3. 串班的 StopTime 歸屬（靠 From 或 To 站）為近似——認領制逐站重設 tExpect，誤差不累積；validator 站間檢查為兜底。
  4. 雜訊時刻（孤立無法成班者，全網個位數）標記為已消化、不產班次——避免無限迴圈，已註解說明。

### Verify 結果
§3 七項全數通過（證據見核取註記）：認領率 99.7–100%、validator 全綠、gzip 221KB 總傳輸。

### Validate 結果
用戶目檢路網通過（「我覺得很好」）；班次量級與北捷公開資訊相符；「同意進 M2」隨 Gate 確認。

### 偏差與學習
1. **三個資料事實**（已回寫 SDD §4.3）：終點群組可超出 S2S 站鏈（R-2 實為北投↔象山）；中途起點出庫車平日約 190 班需 cascade 錨定；G-2 僅平日行駛。
2. **幾何兩坑**（已回寫 T1.2 註記）：縫合需轉角約束、交會站需跨鏈橋接。
3. 估時 7 人日 vs 實際約 1 人日：Spike 的 RouteID 發現讓串班從「最高風險」變成按圖施工；debug 頁提早部署讓目檢零來回。
4. 驗證器誤報教訓：「11 交路天天齊」的假設錯了——營運規則要寫成「按日別的允許缺席清單」，而不是寫死數字。

## 6. 用戶確認（Gate）

- [ ] 已向用戶回報 M1 結果並取得進入 M2 的確認（日期：＿＿＿）
