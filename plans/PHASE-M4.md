# Phase 計畫 — M4：即時校正

| 項目 | 內容 |
|---|---|
| 狀態 | 🟠 檢查完成（待用戶對板 Validate＋Gate） |
| 預估 / 實際耗時 | 5 人日 / 約 0.5 人日 |
| 對應任務 | TASKS.md T4.1–T4.4（含 M0 遞延的 Vercel 環境變數設定） |
| 開始 / 完成日期 | 2026-07-18 / 2026-07-18 |

## 1. 目標（Plan）

1. `/api/live` 代理上線：藏金鑰、瘦身 LiveBoard 為進站事件、CDN 共享快取 15s（使用者數與 TDX 呼叫數脫鉤）。
2. **相位校正**：進站事件 vs 模擬預測 → 每（交路×方向）誤差中位數 → 平滑 offset（每幀 10%、上限 ±180s）；僅「現在模式」（非時間旅行、1×、未暫停）啟用；文湖線靠此拉回精度。
3. `/api/alerts` 告警橫幅（過濾「正常營運」）；TDX 失效靜默退化為純表定＋狀態徽章（即時⚡／表定）。
4. 校正品質記錄：每輪 console 摘要＋`window.__calCsv()` 匯出時序，供調參與 M5 驗收。

**可展示物**：站在月台對板——網頁列車與現場看板同步（誤差中位數 ≤ 45s）。

## 2. 工作項目（Do）

- [x] T4.0 👤 用戶：Vercel 環境變數設定完成（2026-07-18 深夜）
- [x] T4.1 `api/live.ts`＋`api/_tdx.ts`（2026-07-18：真實事件回傳 200；CDN 快取連打 **5/5 HIT**；env 未設時優雅 503 已實測）
- [x] T4.2 `core/calibrate.ts`（2026-07-18：正式站即時咬合——各線 offset +1s～+67s、**文湖線 +201s 被 cap 至 +180**；三輪輪詢數值穩定；含起站折返配對）
- [x] T4.3 `api/alerts.ts`＋橫幅＋徽章（2026-07-18：alerts 過濾正常營運回空清單；徽章 10×→表定、1×→即時⚡ 實測切換）
- [x] T4.4 校正紀錄（2026-07-18：`__calCsv()` 於正式站輸出 30 筆時序；console 每輪摘要）

## 3. Verify — 技術驗證

- [x] 離線驗證：23:30 真實快照 18 事件配對 **72%**（門檻 60%）；各組中位誤差 −12s～+97s
- [x] `/api/live`：回應即時、快取 **5/5 HIT**（首次 MISS 後全命中——TDX 呼叫與使用者數脫鉤實證）
- [x] 校正生效：正式站 `即時⚡` 點亮、CSV 30 筆穩定；倍速切換即停用／恢復（實測）；env 未設時 503 靜默退化（實測）
- [x] 徽章切換正確；無事故時 alerts 空、橫幅不出現
- [x] `lint`／`build`／`validate` 綠燈

## 4. Validate — 需求驗收（對照 PRD F6）

- [ ] **對板實測**（用戶或營運時段截圖比對）：抽站比較網頁列車進站 vs 月台看板／官方 App，誤差中位數 ≤ 45s
- [ ] 文湖線體感改善（synthetic 班次被事件拉正）
- [ ] 用戶同意進 M5（全網打磨上線）

## 5. Check 紀錄（2026-07-18 深夜填寫）

### Code Review
- 範圍：api/{_tdx,live,alerts}、core/calibrate、main 輪詢與 gating、stationboard offset。lint／tsc 零發現。
- 發現與處置：
  1. **Vercel Functions ESM 匯入坑**：root `"type":"module"` 下相對匯入必須帶副檔名——`'./_tdx'` 造成 FUNCTION_INVOCATION_FAILED（import 期崩潰、try/catch 攔不到），改 `'./_tdx.js'`（指向編譯產物）。**規則：Vercel Functions 相對匯入一律 `.js` 副檔名。**
  2. 校正僅在（現在模式 ∧ 資料新鮮 <60s）套用；90s 無資料 offset 緩慢歸零——避免殘留舊校正。
  3. BR +201s 觸 cap ±180：cap 是防瞬移的安全網，殘餘誤差由下輪事件繼續收斂，屬預期行為。
  4. LiveBoard 深夜樣本少（單輪 ~18 筆），中位數對單樣本組（n=1）敏感——白天尖峰樣本多會更穩；必要時可加「n≥2 才生效」門檻（暫不加，觀察 M5）。

### Verify 結果
§3 五項全過（證據見核取註記）。關鍵數字：配對率 72%、快取 5/5 HIT、正式站 offset 表現穩定。

### Validate 結果
待用戶：①營運時段開站看「即時⚡」與列車表現；②（可隔日）任一站對板官方 App，殘餘誤差 ≤45s。

### 偏差與學習
1. 估時 5 人日 vs 實際約 0.5 人日。累計 M0–M4：估 26.5 人日、實際約 3.5 人日。
2. Spike 的「事件式看板」判讀完全正確——校正演算法一次到位，主要成本反而在 Vercel 部署細節（ESM）。
3. 文湖線設計驗證成功：合成班表＋事件校正，深夜誤點 3 分半也能拉回。

## 6. 用戶確認（Gate）

- [ ] 已向用戶回報 M4 結果並取得進入 M5 的確認（日期：＿＿＿）
