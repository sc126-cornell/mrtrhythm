# Phase 計畫 — M4：即時校正

| 項目 | 內容 |
|---|---|
| 狀態 | 🔵 進行中（2026-07-18 M3 Gate 通過後開工） |
| 預估 / 實際耗時 | 5 人日 / — |
| 對應任務 | TASKS.md T4.1–T4.4（含 M0 遞延的 Vercel 環境變數設定） |
| 開始 / 完成日期 | 2026-07-18 / — |

## 1. 目標（Plan）

1. `/api/live` 代理上線：藏金鑰、瘦身 LiveBoard 為進站事件、CDN 共享快取 15s（使用者數與 TDX 呼叫數脫鉤）。
2. **相位校正**：進站事件 vs 模擬預測 → 每（交路×方向）誤差中位數 → 平滑 offset（每幀 10%、上限 ±180s）；僅「現在模式」（非時間旅行、1×、未暫停）啟用；文湖線靠此拉回精度。
3. `/api/alerts` 告警橫幅（過濾「正常營運」）；TDX 失效靜默退化為純表定＋狀態徽章（即時⚡／表定）。
4. 校正品質記錄：每輪 console 摘要＋`window.__calCsv()` 匯出時序，供調參與 M5 驗收。

**可展示物**：站在月台對板——網頁列車與現場看板同步（誤差中位數 ≤ 45s）。

## 2. 工作項目（Do）

- [ ] T4.0 👤 用戶：Vercel dashboard 設 `TDX_CLIENT_ID`／`TDX_CLIENT_SECRET`（M0 遞延項）
- [ ] T4.1 `api/live.ts`＋`api/_tdx.ts`：token 記憶體快取、LiveBoard 瘦身 `[{sta,dest,t}]`、`s-maxage=15, swr=30`、3s 逾時 →503
- [ ] T4.2 `core/calibrate.ts`：事件↔模擬列車配對（同終點含該站、預測到站最近者、|誤差|<300s）、每組中位數、平滑收斂；`positionOf(trip, t−offset)` 接入
- [ ] T4.3 `api/alerts.ts`＋前端橫幅＋狀態徽章（即時⚡／表定）＋失效退化
- [ ] T4.4 校正紀錄：每輪 console 摘要、`__calCsv()`、看板時刻套用 offset

## 3. Verify — 技術驗證

- [ ] 本地離線驗證：以真實 LiveBoard 快照對 tt 資料跑配對數學——事件配對率 ≥60%、各組誤差中位數量級合理（±180s 內）
- [ ] `/api/live` 部署後：回應 <1s、Cache-Control 正確；連打 10 次 `x-vercel-cache` 顯示 HIT 為主
- [ ] 校正生效：現在模式下 offset 隨輪次收斂；時間旅行／倍速時完全停用；TDX 斷線靜默退化
- [ ] 徽章狀態正確切換（即時⚡ ↔ 表定）；告警橫幅在無事故時不出現
- [ ] `lint`／`build`／`validate` 綠燈

## 4. Validate — 需求驗收（對照 PRD F6）

- [ ] **對板實測**（用戶或營運時段截圖比對）：抽站比較網頁列車進站 vs 月台看板／官方 App，誤差中位數 ≤ 45s
- [ ] 文湖線體感改善（synthetic 班次被事件拉正）
- [ ] 用戶同意進 M5（全網打磨上線）

## 5. Check 紀錄（完成後填寫）

### Code Review
（待填）

### Verify 結果
（待填）

### Validate 結果
（待填）

### 偏差與學習
（待填）

## 6. 用戶確認（Gate）

- [ ] 已向用戶回報 M4 結果並取得進入 M5 的確認（日期：＿＿＿）
